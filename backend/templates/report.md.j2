# 面接練習レポート

**日時**: {{ date }}  
**音声ファイル**: `{{ filename }}`

---

## 📝 文字起こし結果

{{ transcript.text }}

{% if transcript.segment_count > 0 %}
**セグメント数**: {{ transcript.segment_count }}  
**音声の長さ**: {{ audio.duration }}秒
{% endif %}

---

## 📊 音響分析

| 項目 | 値 | 評価 |
|------|-----|------|
| **話速** | {{ audio.speech_rate }} 文字/分 | {% if audio.speech_rate > 350 %}少し速いです{% elif audio.speech_rate < 250 %}少しゆっくりです{% else %}適切です{% endif %} |
| **平均音量** | {{ audio.average_volume }} dB | {% if audio.average_volume > -20 %}良好です{% elif audio.average_volume > -30 %}適切です{% else %}少し小さめです{% endif %} |
| **ポーズ回数** | {{ audio.pause_count }} 回 | {% if audio.pause_count > 15 %}やや多めです{% elif audio.pause_count < 5 %}とても少ないです{% else %}適切です{% endif %} |
| **総ポーズ時間** | {{ audio.pause_total_duration }} 秒 | - |
| **平均ピッチ** | {{ audio.pitch_mean }} Hz | - |

### 分析コメント

{% if audio.speech_rate > 350 %}
- 話速が速めです。落ち着いて、一呼吸おいて話すと良いでしょう。
{% elif audio.speech_rate < 250 %}
- 話速がやや遅めです。もう少しテンポよく話すことを意識しましょう。
{% else %}
- 話速は適切です。このペースを維持しましょう。
{% endif %}

{% if audio.pause_count > 15 %}
- ポーズが多めです。事前準備を十分にして、スムーズに話せるようにしましょう。
{% elif audio.pause_count < 5 %}
- ポーズが少ないです。要点を強調する際に、意図的なポーズを入れると効果的です。
{% endif %}

---

## 🎤 音声感情分析

| 項目 | スコア | 評価 |
|------|--------|------|
| **自信度** | {{ emotion.confidence_score|default(50) }}/100 | {% if emotion.confidence_score > 70 %}自信を持って話せています✨{% elif emotion.confidence_score > 50 %}もう少し自信を持ちましょう{% else %}練習を重ねて自信をつけましょう{% endif %} |
| **緊張度** | {{ emotion.nervousness_score|default(50) }}/100 | {% if emotion.nervousness_score > 70 %}かなり緊張しています{% elif emotion.nervousness_score > 50 %}やや緊張気味です{% else %}リラックスして話せています{% endif %} |
| **声の安定性** | {{ emotion.voice_stability|default(50) }}/100 | {% if emotion.voice_stability > 70 %}非常に安定しています{% elif emotion.voice_stability > 50 %}概ね安定しています{% else %}声が不安定です{% endif %} |

### 音声特徴

- **声の震え (ジッター)**: {{ emotion.jitter|default(0) }} ms
  {% if emotion.jitter > 10 %}→ 声が震えています。深呼吸をしてリラックスしましょう。{% elif emotion.jitter > 5 %}→ 少し不安定です。落ち着いて話すことを意識しましょう。{% else %}→ 安定しています。{% endif %}

- **ピッチの変動**: {{ emotion.pitch_variance|default(0) }} Hz
  {% if emotion.pitch_variance > 50 %}→ 声の高さが大きく変動しています。{% elif emotion.pitch_variance > 30 %}→ やや変動が見られます。{% else %}→ 安定しています。{% endif %}

- **エネルギーの変動**: {{ emotion.energy_variance|default(0) }}
  {% if emotion.energy_variance > 0.1 %}→ 音量が不安定です。一定の声量を保ちましょう。{% else %}→ 音量が安定しています。{% endif %}

### 感情フィードバック

{{ emotion_feedback|default('感情分析結果がありません') }}

---

## 🤖 AI 分析結果

### キーワード

{% if ai.keywords %}
{% for keyword in ai.keywords %}
- {{ keyword }}
{% endfor %}
{% else %}
キーワードが検出されませんでした。
{% endif %}

### 敬語チェック

{{ ai.keigo_feedback }}

### 感情分析

| 項目 | スコア | 評価 |
|------|--------|------|
| **自信度** | {{ ai.confidence_score }}/100 | {% if ai.confidence_score > 70 %}自信を持って話せています✨{% elif ai.confidence_score > 50 %}もう少し自信を持ちましょう{% else %}練習を重ねて自信をつけましょう{% endif %} |
| **緊張度** | {{ ai.nervousness_score }}/100 | {% if ai.nervousness_score > 70 %}かなり緊張しています{% elif ai.nervousness_score > 50 %}やや緊張気味です{% else %}リラックスして話せています{% endif %} |

### 全体的な印象

{{ ai.overall_impression }}

---

## 🎥 映像姿勢・視線分析

{% if video.available %}
| 指標 | スコア | コメント |
|------|--------|-----------|
| 姿勢スコア | {{ video.posture_score|default('N/A') }} | {% if video.posture_score %}{% if video.posture_score > 80 %}姿勢がとても安定しています{% elif video.posture_score > 60 %}概ね良好です{% else %}背筋と肩のラインを意識しましょう{% endif %}{% else %}データ不足のため算出できません{% endif %} |
| 視線スコア | {{ video.eye_contact_score|default('N/A') }} | {% if video.eye_contact_score %}{% if video.eye_contact_score > 80 %}カメラをしっかり見られています{% elif video.eye_contact_score > 60 %}ほぼカメラを捉えています{% else %}目線が逸れがちです。面接官を見る意識を持ちましょう{% endif %}{% else %}データ不足のため算出できません{% endif %} |

{% if video.overlay_image %}
![骨格オーバーレイ]({{ video.overlay_image }})
{% endif %}

解析対象フレーム数: {{ video.frames_analyzed }} / コメント: {{ video.message }}

{% else %}
映像分析は実行されていません。理由: {{ video.message|default('映像データが存在しません。') }}
{% endif %}

---

## 💬 教師コメント

<!-- ここに教師がコメントを追加してください -->

**指導ポイント**:
- 
- 
- 

**次回までの課題**:
1. 
2. 
3. 

---

## 📈 改善のヒント

### 話し方
- 明瞭に発音する
- 適度なスピードで話す (300文字/分程度)
- 要点の前後で間を取る

### 内容
- 具体的なエピソードを交える
- 結論→理由→具体例の順で話す (PREP法)
- 志望動機と自分の強みを関連付ける

### 態度
- 笑顔を意識する
- 相手の目を見て話す (視線を3秒ずつ)
- 姿勢を正しく保つ

---

**生成日時**: {{ date }}  
**システム**: 圧勝面接練習システム v1.0
